services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_PATH=/app/data/verifyhuman.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
