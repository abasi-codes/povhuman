{
  "name": "verifyhuman",
  "version": "0.2.0",
  "description": "Livestream verification for human task completion — AI agents verify real-world work via live video + Trio VLM",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "lint": "eslint .",
    "build:all": "npm run build && npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "dev": "tsx watch src/server.ts",
    "dev:backend": "tsx watch src/server.ts",
    "dev:frontend": "cd frontend && npm run dev",
    "start": "node dist/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "cd e2e && npx playwright test",
    "compile:contracts": "TS_NODE_PROJECT=tsconfig.hardhat.json npx hardhat compile",
    "deploy:contract": "TS_NODE_PROJECT=tsconfig.hardhat.json npx hardhat run scripts/deploy.ts --network 0g-testnet"
  },
  "dependencies": {
    "@0glabs/0g-ts-sdk": "^0.3.3",
    "@hono/node-server": "^1.8.0",
    "better-sqlite3": "^11.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.13.1",
    "hono": "^4.0.0",
    "nanoid": "^5.0.0",
    "pino": "^9.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@types/better-sqlite3": "^7.6.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^2.1.9",
    "eslint": "^9.0.0",
    "hardhat": "^2.28.6",
    "ts-node": "^10.9.2",
    "tsx": "^4.7.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.0.0"
  }
}
